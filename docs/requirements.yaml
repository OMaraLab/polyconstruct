name: polyconstruct-docs
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip
  - mdanalysis-sphinx-theme >=1.0.1

  # MDAnalysis
  - MDAnalysis

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
